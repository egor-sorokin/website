How to run the flask part properly:
```
python2 -m virtualenv venv
. venv/bin/activate
pip install -r requirements.txt
flask run
```

To remove created virtual env being inside of it:
```
deactivate
rm -r venv
```
